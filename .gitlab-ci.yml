# README: http://docs.gitlab.com/ce/ci/yaml/README.html
variables:
  REMOTE: 'ssh -l capistrano'

stages:
  - deploy
  
before_script:
  - uname -a
  - whoami
  - hostname
  - pwd
  
after_script:
  - echo 'after_script executing'
  - exit 0
  
deploy_to_prod:
  stage: deploy
  variables:
    DEPLOYHOST: mx.forskningsdatabasen.dk
    DEPLOYDIR: /var/www/mxd
    TMPDIR: /tmp/mxd.tmp
  when: manual
  script:
    - $REMOTE "rm -rf $TMPDIR"
    - rsync -avu --delete site/mxd/ capistrano@$DEPLOYHOST:$TMPDIR
    - $REMOTE $DEPLOYHOST "chmod -R g+rx $TMPDIR"
    - $REMOTE $DEPLOYHOST "chgrp -R www-data $TMPDIR"
    - $REMOTE "sudo rm -rf $DEPLOYDIR.old"
    - $REMOTE "sudo mv $TMPDIR $DEPLOYDIR"
